for start server

        >> cd food-app/backend
        >> npm install
        >> nodemon index.js

for starting frontend
        
        >> cd food-app
        >> npm install
        >> npm start

## ADD json file "foodData" into ur MongoDB database
